<Page
    x:Class="TCC_Aline.Pages.CategoriaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TCC_Aline.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:TCC_Aline.Model"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Name="ItemGridViewTemplate" x:DataType="data:Tipo">
            <RelativePanel>
                <Image x:Name="image" Tapped="image_Tapped" Source="{x:Bind Imagem}"/>
            </RelativePanel>
        </DataTemplate>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="50 10">
        <GridView x:Name="Itens" ItemTemplate="{StaticResource ItemGridViewTemplate}" SelectionMode="None">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="20"/>
                    <Setter Property="Width" Value="200"/>
                </Style>
            </GridView.ItemContainerStyle>
        </GridView>
    </Grid>
</Page>
