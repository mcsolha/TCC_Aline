<Page
    x:Class="TCC_Aline.Pages.Gloss"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:TCC_Aline.UserControls"
    xmlns:data="using:TCC_Aline.Model"
    xmlns:local="using:TCC_Aline.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="itemExpand" x:DataType="data:GlossarioData">
            <StackPanel>
            <controls:itemExpand Texto="{x:Bind Texto}" Imagem="{x:Bind Imagem}" Nome="{x:Bind Nome}" IsExpanded="False"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="0,10,0,0">
        <ListView x:Name="gloss" ItemTemplate="{StaticResource itemExpand}" SelectionMode="None" Padding="50,0,300,0">
        </ListView>
    </Grid>
</Page>
