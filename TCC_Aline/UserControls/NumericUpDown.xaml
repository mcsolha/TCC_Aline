<UserControl
    x:Class="TCC_Aline.UserControls.NumericUpDown"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TCC_Aline.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="35"
    d:DesignWidth="400">

    <RelativePanel>
        <TextBox Text="{x:Bind Path=Porcoes,Mode=TwoWay}" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" IsReadOnly="True"/>
        <Grid RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:Name="add" Grid.Row="0" Click="add_Click" Padding="12,4" Background="Transparent">
                <Viewbox>
                    <SymbolIcon Symbol="Add"/>
                </Viewbox>
            </Button>
            <Button x:Name="remove" Grid.Row="1" Click="remove_Click" Padding="12,4" Background="Transparent">
                <Viewbox>
                    <SymbolIcon Symbol="Remove"/>
                </Viewbox>
            </Button>
        </Grid>
    </RelativePanel>
</UserControl>
